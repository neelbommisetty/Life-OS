#!/usr/bin/env sh
set -eu

if [ ! -x "./node_modules/.bin/commitlint" ]; then
  echo "commitlint is not installed. Run 'bun install' and retry." >&2
  exit 1
fi

./node_modules/.bin/commitlint --edit "$1"
