export function getTaskCardActionLabels(title: string) {
  return {
    edit: `Edit ${title}`,
    editCta: "Edit task",
    move: `Move ${title}`,
    moveCta: "Move",
    delete: `Delete ${title}`,
  };
}
